# 国际化

应用使用 next-intl；网址路径与邮件/API协议保持不变。

## 语言选择

登录页面与侧边栏底部提供“自动 / English / 简体中文”。默认自动，根据浏览器请求的 `Accept-Language` 顺序和权重选择已支持语言；所有 `zh-*` 变体使用简体中文，未支持的语言回退英文。手动偏好通过 `mailflare-language` Cookie 保存一年。自动模式在每次请求重新读取浏览器语言。

服务端按请求解析语言，不共享可变的当前语言状态。客户端切换翻译后刷新服务端组件，保留当前网址和客户端表单状态。邮件正文、主题、联系人姓名、自定义文件夹与用户输入保持原样。

## 添加与使用文案

- 同步组件：`const t = useT()`，从 `@/i18n/use-t` 导入。
- 异步服务端组件：`const t = await getT()`，从 `@/i18n/server` 导入。
- 只在显示系统文案时调用 `t("Inbox")`；不要包装用户内容、协议枚举、URL 或程序判断用的原始错误。
- 动态句子使用完整 ICU 消息，例如 `t("Hello {name}", { name })`；计数可用 ICU `plural`，避免英文词尾或碎片拼接。
- 日期和数字使用 next-intl 的 `useFormatter`，或给纯格式化函数显式传入当前 locale。时间显示保留浏览器时区。

字典集中在 `src/i18n/messages/en.json` 和 `zh-CN.json`。运行 `mise run i18n:extract` 汇总英文源文案，审阅新增词条后，只需翻译中文 JSON。英文通过 `src/i18n/catalog.ts` 中的 `messageKey` 生成稳定 ID（UTF-16 FNV-1a）；相同系统文案复用一个词条。两份字典的键与 ICU 参数必须一致。语言切换器使用少量显式命名键。

提取器同时收集静态导航、状态与已知错误消息，方便翻译人员集中处理。新增动态状态标签后，应检查该标签已进入英文词条；不得将用户提供的名字或邮件内容作为翻译输入。

缺失或空的中文词条回退英文。后端和第三方错误保留原始协议内容，在前端显示边界翻译；字典里已知的简单参数模板可匹配已插值的错误，未知详情保留原文。

## 新增语言

1. 在 `src/i18n/config.ts` 中注册 locale，并完善自动语言匹配规则。
2. 新建与英文键集合一致的翻译 JSON，在 `catalog.ts` 注册字典和翻译器。
3. 在语言切换器加入该语言的自称名称。
4. 运行 `mise run i18n:check`、`mise run typecheck` 和 `mise run build`。
5. 验证登录、收件箱、写信、设置与管理后台的窄屏布局，并确认用户内容不变。

`i18n:check` 校验所有翻译键、ICU 语法及参数一致性，并检查源码中的静态 `t(...)` 调用是否有对应英文词条。它不替代浏览器验收。
